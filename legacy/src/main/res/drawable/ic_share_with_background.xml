<layer-list
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:drawable="@drawable/background_circle" android:width="32dp" android:height="32dp"/>

    <!-- the icon is not symmetric, not placing it in the center is deliberate -->
    <item android:drawable="@drawable/ic_share"
        android:top="4dp"
        android:bottom="4dp"
        android:left="2dp"
        android:right="6dp"/>

</layer-list>